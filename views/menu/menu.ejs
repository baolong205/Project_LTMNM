<%- include('../partials/header') %>

<div class="container mt-4">
    <h2 class="text-center mb-4">🍽️ Menu</h2>

    <!-- Bảng danh sách món ăn -->
    <% if (menuItems && menuItems.length > 0) { %>
        <div class="table-responsive">
            <table class="table table-hover table-bordered align-middle text-center">
                <thead class="table-dark">
                    <tr>
                        <th>Loại món</th>
                        <th>Mã món</th>
                        <th>Tên món</th>
                        <th>Nhóm thực đơn</th>
                        <th>Đơn vị tính</th>
                        <th>Giá</th>
                    </tr>
                </thead>
                <tbody>
                    <% menuItems.forEach(item => { %>
                        <tr>
                            <td><%= item.category %></td>
                            <td><%= item.code %></td>
                            <td><%= item.name %></td>
                            <td><%= item.group %></td>
                            <td><%= item.unit %></td>
                            <td class="fw-bold"><%= item.price ? item.price.toLocaleString('vi-VN') : 'N/A' %> VND</td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    <% } else { %>
        <p class="text-center text-danger fs-5">❌ Không có món nào trong menu.</p>
    <% } %>
</div>

<%- include('../partials/footer') %>
