<%- include('../partials/header') %>

<div class="container mt-4">
    <h3 class="text-center">🧾 Danh sách bàn cần thanh toán</h3>

    <% if (tableNumbers.length > 0) { %>
        <ul class="list-group">
            <% tableNumbers.forEach(table => { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span>Bàn số <strong><%= table %></strong></span>
                    <a href="/payment/<%= table %>" class="btn btn-success btn-sm">Thanh toán</a>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <div class="alert alert-info text-center mt-4">Hiện chưa có bàn nào đặt món.</div>
    <% } %>
</div>

<%- include('../partials/footer') %>
