<%- include('../partials/header') %>

<div class="container mt-4">
    <h3 class="text-center mb-4">🧾 Danh sách bàn cần thanh toán</h3>

    <% if (tableNumbers.length > 0) { %>
        <ul class="list-group shadow-sm">
            <% tableNumbers.forEach(table => { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span class="fs-5">🪑 Bàn số <strong><%= table %></strong></span>
                    <a href="/payment/<%= table %>" class="btn btn-success px-4">Thanh toán</a>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <div class="alert alert-info text-center mt-4 fs-5">
            📭 Hiện chưa có bàn nào cần thanh toán.
        </div>
    <% } %>
</div>

<%- include('../partials/footer') %>
