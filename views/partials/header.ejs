<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quán Lý Café</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    body {
      background-color: #e6d2b5 !important;
    }

    /* CSS cho thanh nav */
    .navbar {
      background-color: #4a2c2a !important; /* Nâu đậm */
      padding: 10px 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
    }

    .navbar .navbar-brand {
      display: flex;
      align-items: center;
      color: #fff !important; /* Màu trắng cho logo */
      font-size: 1.5rem;
      font-weight: bold;
      text-decoration: none;
    }

    .navbar .navbar-brand i {
      margin-right: 8px;
      font-size: 1.4rem;
    }

    .navbar .user-info {
      color: #fff;
      font-size: 0.9rem;
    }

    .navbar .user-info span {
      margin-left: 10px;
    }

    .navbar .user-info .status {
      color: #f4a261; /* Màu vàng nhạt cho "Đang xuất" và "Đăng xuất" */
      text-decoration: none; /* Bỏ gạch chân cho liên kết */
    }

    /* Ẩn các phần không cần thiết */
    .navbar-nav,
    .navbar-toggler {
      display: none !important;
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fas fa-coffee"></i>
        Quán Lý Café
      </a>
      <div class="user-info">
        <% if (session && session.user) { %>
          <span><%= session.user.username %> (<a href="/auth/logout" class="status">Đăng xuất</a>)</span>
          <span>Vai trò: <%= session.user.staffRole || 'admin' %></span>
        <% } else { %>
          <span>Chưa đăng nhập</span>
        <% } %>
      </div>
    </div>
  </nav>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>